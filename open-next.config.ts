import { defineCloudflareConfig } from "@opennextjs/cloudflare";

export default defineCloudflareConfig({
  // OpenNext Cloudflare configuration
  // Most settings work out of the box, but you can customize here
  
  // If you need to customize the build output
  // buildCommand: "npm run build",
  
  // If you need to specify a custom output directory
  // outputDir: ".open-next",
});
